# Copy to .env.local for local dev. NEVER commit real values.
# Only needed if the selected design requires a server-side LLM proxy.
# Preferred architecture is client-side inference with NO key at all: the
# deployed demo may be opened by judges days after submission, unattended.
FIREWORKS_API_KEY=
